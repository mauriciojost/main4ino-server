server {
  host = "0.0.0.0"
  port = 8080
}

database {
  driver = "org.h2.Driver"
  url = "jdbc:h2:mem:test-db"
  user = "sa"
  password = ""
  cleanup {
    period-secs = 1
    retention-secs = 10
  }
}

dev-logger {
  logs-base-path = "/tmp"
}

firmware {
  firmware-base-path = "src/test/resources/firmwares/1/"
}
